/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // e.g. if you need to allow external image domains:
  images: {
    domains: ['images.squarespace-cdn.com'],
  },
};

export default nextConfig;

