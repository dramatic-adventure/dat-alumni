// MOCKUP ONLY — /dev/donate-mockups/option-c (safe to delete)
import { Suspense } from "react";
import OptionC from "./OptionC";

export default function OptionCPage() {
  return (
    <Suspense fallback={null}>
      <OptionC />
    </Suspense>
  );
}
